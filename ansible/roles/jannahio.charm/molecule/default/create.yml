---
- name: Create
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: "Create multipass environment"
      block:
        - name: "Import multipass infra tasks"
          include_tasks: '{{ item }}'
          with_fileglob:
            - ../../tasks/multipass/main.yml
          tags:
            - "multipass_infra"
            - "operator_e2e"
      rescue:
        - name: "Create multipass exception is thrown"
          ansible.builtin.debug:
            msg: 'Create multipass exception is thrown'

        - name: Re-emit failure
          vars:
            failed_task:
              result: '{{ ansible_failed_result }}'
          fail:
            msg: '{{ failed_task }}'
