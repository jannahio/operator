apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: jannah-web
spec:
  hosts:
  - "*"
  gateways:
  - kubeflow-gateway
  http:
  - match:
    - uri:
        exact: /
    - uri:
        prefix: /static
    - uri:
        exact: /login
    - uri:
        exact: /logout
    - uri:
        prefix: /register
    - uri:
        prefix: /workflows
    route:
    - destination:
        host: jannah-web
        port:
          number: 8100